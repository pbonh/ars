---
- name: Sidecar | Ensure config directory exists
  ansible.builtin.file:
    path: "{{ sidecar_config_dir }}"
    state: directory
    mode: "0755"

- name: Sidecar | Write config.json
  ansible.builtin.template:
    src: "sidecar/config.json.j2"
    dest: "{{ sidecar_config_path }}"
